{
  "name": "mcp-pr-messages-generator",
  "version": "1.0.0",
  "description": "MCP Server for generating Pull Request messages based on Git commits and changes",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "mcp-pr-messages-generator": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod +x build/index.js",
    "start": "node build/index.js",
    "dev": "tsc && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "pull-request",
    "git",
    "automation",
    "pr-messages"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.1",
    "@types/node": "^24.0.4",
    "simple-git": "^3.28.0",
    "zod": "^3.25.67"
  }
}
